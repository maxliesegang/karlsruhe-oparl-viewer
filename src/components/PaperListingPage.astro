---
import type {
  Paper,
  PaperFilterMetadata,
  PaperFilterOptions,
} from "../shared/types";
import PageContainer from "./PageContainer.astro";
import PapersTable from "./PapersTable.astro";

interface Props {
  title: string;
  papers: Paper[];
  filterMetadataByReference?: Record<string, PaperFilterMetadata>;
  filterOptions?: PaperFilterOptions;
}

const { title, papers, filterMetadataByReference, filterOptions } = Astro.props;
---

<PageContainer>
  <h1 class="page-header">{title}</h1>
  <slot name="intro" />
  <div data-pagefind-ignore>
    <PapersTable
      papers={papers}
      filterMetadataByReference={filterMetadataByReference}
      filterOptions={filterOptions}
    />
  </div>
</PageContainer>
